# data/

Git-ignored — datasets are never committed, only this README. `load_watches()` finds the
data automatically: the mounted dataset on Kaggle, or a Kaggle download on Colab / local.

To use a local copy without downloading, drop `Watches.csv` into `data/raw/`.
